// Import the Express application instance from the parent directory's server.ts file
import { app } from '../server';
// Export the app instance as the default export for Vercel's serverless function entry point
export default app;
